# Output shell completion code for the specified shell. For bash, load the completion code into the current shell: 
	
# 	source <(kubeless completion bash)

# Usage:
#   kubeless completion [command]

# Available Commands:
#   bash        output shell completion code for bash

# Flags:
#   -h, --help   help for completion

# Use "kubeless completion [command] --help" for more information about a command.
